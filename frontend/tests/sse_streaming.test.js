import { describe, it, expect } from "vitest";

// useStreamingRenderer 已重构为 useStreamPipeline.ts (API 不兼容)，此文件需重写
describe.skip("useStreamingRenderer — 已被 useStreamPipeline 替代", () => {
  it.skip("所有测试待重写", () => {});
});
