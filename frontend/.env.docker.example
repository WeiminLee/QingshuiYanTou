# Docker 部署环境变量（与 docker-compose.yml 的 API_KEY 对齐）
# 注意：Vite 在构建时烘焙 env 变量，因此 Docker 构建时需传入这些值。
# 开发时使用 .env.local 中的 mock 模式。
VITE_API_KEY=dev-docker-key
VITE_USE_MOCK_AGENT=false
VITE_API_BASE_URL=/api/v1